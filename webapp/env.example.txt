# Copy this file to .env and fill in the values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Service role key (server-side only, NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# App Configuration
APP_NAME="AI Chat"
OPENAI_MODEL=gpt-4

# N8N Webhook Configuration (Server-side only)
# TODO: Set your n8n webhook URL here
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# Optional: Webhook secret for authentication
# TODO: If your n8n workflow requires secret, set it here
N8N_WEBHOOK_SECRET=your_webhook_secret_here

# TODO: After setting up:
# 1. Copy this file to .env and fill in the values
# 2. Enable Google OAuth provider in Supabase Dashboard
# 3. Set OAuth redirect URLs:
#    - Local: http://localhost:3008/auth/callback
#    - Production: https://yourdomain.com/auth/callback
# 4. Run SQL migration in Supabase SQL Editor (see supabase/migrations/001_init.sql)
# 5. Adjust n8n webhook payload/response format if needed (see /api/chat/route.ts)


